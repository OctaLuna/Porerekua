# URL base del backend (NestJS). Incluye el prefijo /api.
VITE_API_URL=http://localhost:3333/api

# API key de MapTiler para el mapa (GeoreferencingPage). Restringir por dominio en el panel de MapTiler.
VITE_MAPTILER_KEY=tu_maptiler_key
